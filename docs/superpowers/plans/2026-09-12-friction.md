# Friction réduite — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Réduire les gestes entre l'envie de faire quelque chose et le fait de le faire : raccourci global, reprise de la dernière pratique, enchaînement de Pomodoros sur des engagements différents, et un widget d'agenda posé sur le bureau.

**Architecture:** Trois des quatre pièces sont de petits ajouts aux surfaces existantes. Le widget de bureau reprend trait pour trait la technique déjà éprouvée de l'overlay Pomodoro : une fenêtre Electron sans bordure, alimentée par IPC depuis la fenêtre principale, qui ne parle jamais à Supabase elle-même.

**Tech Stack:** Electron + React 19 + TypeScript + Tailwind + Supabase + Vitest.

**Spec:** `docs/superpowers/specs/2026-09-12-daily-tool-backlog-design.md` (chantier 6)

**Aucune migration.** Chantier intégralement vérifiable, à l'exception du comportement natif (raccourci global, fenêtre widget, menu du tray).

## Déjà livré, ne pas réimplémenter

La cinquième idée du groupe — « comparaison de temps investi entre engagements » — **a été livrée au chantier 1** sous la forme du widget « Répartition par engagement » de l'écran Bilan. Elle n'apparaît donc pas dans ce plan.

## Global Constraints

- Toute copie visible est **en français**.
- Palette limitée aux jetons existants de `src/renderer/src/theme/colors.ts`.
- **Aucune nouvelle dépendance npm.**
- Logique pure dans `src/renderer/src/lib/*.ts`, testée avec Vitest. **Aucun test de composant React.**
- Toute erreur remontée à l'écran (`role="alert"`, `text-danger`), jamais avalée.
- Accessibilité : `aria-label` sur tout contrôle sans libellé visible, anneau de focus visible.
- **Trois migrations antérieures ne sont pas encore appliquées à la base live.** Aucune requête ne doit nommer `deleted_at`, `reminder_lead_minutes` ni `weekly_review_dismissed_at` : utiliser `select('*')` et filtrer côté client.
- La route réelle de la nouvelle entrée est **`/entree/nouvelle`** (singulier), pas `/entrees/nouvelle`.

---

### Task 1: Raccourci clavier global

**Files:**
- Modify: `src/main/index.ts`
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`
- Modify: `src/renderer/src/App.tsx`

**Interfaces:**
- Produces: `window.api.onNavigateRequest(callback) => unsubscribe`, et la navigation déclenchée par `Ctrl+Alt+N`

- [ ] **Step 1: Enregistrer le raccourci dans le process principal**

Dans `src/main/index.ts`, importer `globalShortcut` depuis `electron`, puis ajouter dans le bloc `app.whenReady().then(…)`, après les autres branchements :

```ts
    // Combinaison fixe pour ce premier jet. `register` renvoie false si une
    // autre application détient déjà le raccourci — on le trace sans
    // insister : l'app reste parfaitement utilisable sans lui.
    const registered = globalShortcut.register('CommandOrControl+Alt+N', () => {
      if (!mainWindow) return;
      mainWindow.show();
      mainWindow.focus();
      mainWindow.webContents.send('navigate:request', '/entree/nouvelle');
    });
    if (!registered) {
      console.warn('Raccourci global Ctrl+Alt+N indisponible (déjà pris par une autre application).');
    }
```

Et, au niveau du module, libérer le raccourci à la fermeture — Electron l'exige explicitement :

```ts
app.on('will-quit', () => {
  globalShortcut.unregisterAll();
});
```

- [ ] **Step 2: Relayer vers le renderer**

Dans `src/preload/index.ts`, objet `api` :

```ts
  onNavigateRequest: (callback: (path: string) => void): (() => void) => {
    const listener = (_event: Electron.IpcRendererEvent, path: string) => callback(path);
    ipcRenderer.on('navigate:request', listener);
    return () => ipcRenderer.removeListener('navigate:request', listener);
  },
```

Dans `src/renderer/src/env.d.ts`, interface `SaintDailyApi` :

```ts
  onNavigateRequest: (callback: (path: string) => void) => () => void;
```

- [ ] **Step 3: Écouter côté renderer**

Dans `src/renderer/src/App.tsx`, le composant `EngagementWatchers` (rendu par `AppProvidersLayout`, donc à l'intérieur du routeur et de `AuthGate`) est l'endroit naturel. Y ajouter :

```tsx
  const navigate = useNavigate();
  useEffect(() => window.api?.onNavigateRequest?.((path) => navigate(path)), [navigate]);
```

en important `useEffect` de `react` et `useNavigate` de `react-router-dom`.

- [ ] **Step 4: Vérifier**

Run: `npm run typecheck` puis `npm test -- --run`
Expected: aucune erreur, les 137 tests existants toujours verts.

- [ ] **Step 5: Commit**

```bash
git add src/main/index.ts src/preload/index.ts src/renderer/src/env.d.ts src/renderer/src/App.tsx
git commit -m "feat: add a global shortcut that jumps straight to a new entry"
```

---

### Task 2: Reprendre où j'en étais

**Files:**
- Modify: `src/renderer/src/lib/streaks.ts`
- Modify: `src/renderer/src/lib/streaks.test.ts`
- Modify: `src/renderer/src/screens/Accueil.tsx`

**Interfaces:**
- Produces: `lastPracticedEngagementId(entriesByEngagement)`

- [ ] **Step 1: Ajouter les tests**

Dans `src/renderer/src/lib/streaks.test.ts`, ajouter :

```ts
describe('lastPracticedEngagementId', () => {
  it('returns null when nothing was ever practised', () => {
    expect(lastPracticedEngagementId({})).toBeNull();
    expect(lastPracticedEngagementId({ a: [] })).toBeNull();
  });

  it('returns the engagement holding the most recent entry', () => {
    expect(
      lastPracticedEngagementId({
        a: [{ practicedAt: '2026-09-01T09:00:00Z' }, { practicedAt: '2026-09-05T09:00:00Z' }],
        b: [{ practicedAt: '2026-09-03T09:00:00Z' }],
      })
    ).toBe('a');
  });

  it('is not fooled by the order the engagements are listed in', () => {
    expect(
      lastPracticedEngagementId({
        b: [{ practicedAt: '2026-09-09T09:00:00Z' }],
        a: [{ practicedAt: '2026-09-05T09:00:00Z' }],
      })
    ).toBe('b');
  });
});
```

et compléter l'import en tête de fichier avec `lastPracticedEngagementId`.

- [ ] **Step 2: Lancer les tests pour les voir échouer**

Run: `npm test -- --run src/renderer/src/lib/streaks.test.ts`
Expected: FAIL — la fonction n'existe pas.

- [ ] **Step 3: Écrire la fonction**

Dans `src/renderer/src/lib/streaks.ts`, à la suite des autres fonctions :

```ts
/** Engagement dont l'entrée la plus récente est la plus récente de toutes.
 *  null si rien n'a jamais été pratiqué. */
export function lastPracticedEngagementId(
  entriesByEngagement: Record<string, PracticeEntryLike[]>
): string | null {
  let bestId: string | null = null;
  let bestAt = '';
  for (const [engagementId, entries] of Object.entries(entriesByEngagement)) {
    for (const entry of entries) {
      // Comparaison lexicographique d'ISO-8601 : équivalente à l'ordre
      // chronologique et sans conversion de date à chaque entrée.
      if (entry.practicedAt > bestAt) {
        bestAt = entry.practicedAt;
        bestId = engagementId;
      }
    }
  }
  return bestId;
}
```

- [ ] **Step 4: Brancher le bouton sur l'Accueil**

Dans `src/renderer/src/screens/Accueil.tsx`, importer `lastPracticedEngagementId`, puis calculer :

```tsx
  const resumeSkill = useMemo(() => {
    const id = lastPracticedEngagementId(entriesBySkill);
    return id ? (activeSkills.find((s) => s.id === id) ?? null) : null;
  }, [entriesBySkill, activeSkills]);
```

et, dans la rangée d'actions en haut de l'écran (à côté de « Démarrer un pomodoro » et « + Nouvelle entrée »), ajouter — **seulement si `resumeSkill` existe**, pour ne pas afficher un bouton mort à un compte neuf :

```tsx
        {resumeSkill && (
          <Link
            to={`/pomodoro?skillId=${resumeSkill.id}`}
            className={buttonClassName('secondary', 'sm')}
            title={`Reprendre ${resumeSkill.name}`}
          >
            Reprendre {resumeSkill.name}
          </Link>
        )}
```

- [ ] **Step 5: Vérifier**

Run: `npm run typecheck` puis `npm test -- --run`
Expected: tout vert.

- [ ] **Step 6: Commit**

```bash
git add src/renderer/src/lib/streaks.ts src/renderer/src/lib/streaks.test.ts src/renderer/src/screens/Accueil.tsx
git commit -m "feat: offer to resume the most recently practised skill"
```

---

### Task 3: Enchaîner un Pomodoro sur un autre engagement

**Files:**
- Modify: `src/renderer/src/lib/pomodoro.tsx`
- Modify: `src/renderer/src/screens/Pomodoro.tsx`

**Interfaces:**
- Produces: `switchEngagement(skillId, skillName)` sur le contexte Pomodoro

> **Lire `stopInternal` en entier avant de commencer.** C'est la partie la plus délicate du dépôt : elle consolide les checkpoints d'une session en une seule entrée de pratique. Une erreur ici n'échoue pas bruyamment, elle **attribue du temps de pratique au mauvais engagement** — une corruption d'historique silencieuse et difficile à repérer après coup.

- [ ] **Step 1: Extraire la consolidation, sans changer son comportement**

Dans `stopInternal`, tout ce qui suit la lecture des refs — calcul de `partialMinutes`, insertion du checkpoint partiel, relecture des checkpoints, consolidation en une entrée, suppression des checkpoints — doit être déplacé tel quel dans une fonction dédiée :

```ts
  /**
   * Solde la session en cours : convertit les checkpoints accumulés en une
   * unique entrée de pratique pour l'engagement **actuellement ciblé**, puis
   * supprime les checkpoints.
   *
   * Extrait de `stopInternal` sans changement de comportement, pour que le
   * changement d'engagement en cours de session puisse solder l'ancien avant
   * de basculer. Sans ce solde, la consolidation finale attribuerait tout le
   * temps de la session au dernier engagement ciblé, y compris les cycles
   * faits sur le précédent.
   */
  async function flushSession(
    current: PomodoroSession,
    currentDurations: PomodoroDurations,
    currentAuthSession: Session
  ): Promise<void> {
    // … corps déplacé depuis stopInternal, à l'identique …
  }
```

`stopInternal` l'appelle ensuite avant de remettre la session à null. **Vérifier que le comportement d'arrêt est strictement inchangé** : mêmes insertions, mêmes suppressions, même gestion d'erreur, même ordre.

- [ ] **Step 2: Ajouter `switchEngagement`**

```ts
  /**
   * Change l'engagement ciblé sans interrompre le minuteur. Le temps déjà
   * accumulé est soldé au profit de l'engagement qu'on quitte, et la session
   * repart avec une liste de checkpoints vide.
   */
  async function switchEngagement(skillId: string, skillName: string) {
    const current = sessionRef.current;
    const currentDurations = durationsRef.current;
    const currentAuthSession = authSessionRef.current;
    if (!current || !currentDurations || !currentAuthSession) return;
    if (current.skillId === skillId) return;
    setError(null);
    await flushSession(current, currentDurations, currentAuthSession);
    setNote('');
    setSession((session) =>
      session ? { ...session, skillId, skillName, loggedEntryIds: [] } : session
    );
  }
```

Exposer `switchEngagement` dans la valeur du contexte et dans son type.

- [ ] **Step 3: Offrir le changement entre deux phases**

Dans `src/renderer/src/screens/Pomodoro.tsx`, le changement n'est proposé **que lorsque la session attend une confirmation d'avance** (`status === 'awaitingAdvance'`) : en pleine phase de travail, basculer découperait le cycle en cours en deux entrées, ce qui n'est pas ce que veut dire « enchaîner ».

Ajouter, dans le rendu de la session en cours, sous les contrôles existants et uniquement si `session.status === 'awaitingAdvance'` :

```tsx
            <div className="mt-4 flex flex-col gap-2 border-t border-ink-700 pt-4">
              <label htmlFor="pomodoro-switch" className="font-data text-[11px] uppercase tracking-[0.1em] text-muted">
                Enchaîner sur un autre engagement
              </label>
              <select
                id="pomodoro-switch"
                value=""
                onChange={(e) => {
                  const next = activeSkills.find((s) => s.id === e.target.value);
                  if (next) void switchEngagement(next.id, next.name);
                }}
                className={`w-full border border-ink-700 bg-ink-800 px-3 py-2 text-[13px] text-champagne ${FOCUS_RING}`}
              >
                <option value="">Continuer sur {session.skillName}</option>
                {activeSkills
                  .filter((s) => s.id !== session.skillId)
                  .map((s) => (
                    <option key={s.id} value={s.id}>
                      {s.name}
                    </option>
                  ))}
              </select>
              <p className="text-[12px] text-muted">
                Le temps déjà fait est enregistré sur {session.skillName} avant de basculer.
              </p>
            </div>
```

- [ ] **Step 4: Vérifier**

Run: `npm run typecheck` puis `npm test -- --run`
Expected: tout vert, les 19 tests de `pomodoroLogic` inclus (la logique pure n'est pas touchée).

- [ ] **Step 5: Commit**

```bash
git add src/renderer/src/lib/pomodoro.tsx src/renderer/src/screens/Pomodoro.tsx
git commit -m "feat: chain a pomodoro onto another engagement between phases"
```

---

### Task 4: Widget agenda posé sur le bureau

**Files:**
- Create: `src/main/agendaWidget.ts`
- Modify: `src/main/index.ts`
- Modify: `src/main/tray.ts`
- Modify: `src/preload/index.ts`
- Modify: `src/renderer/src/env.d.ts`
- Create: `src/renderer/src/screens/AgendaWidget.tsx`
- Modify: `src/renderer/src/App.tsx`
- Create: `src/renderer/src/hooks/useAgendaWidgetFeed.ts`

**Interfaces:**
- Produces: la fenêtre widget, le canal `agenda:state`, et la route `/agenda-widget`

> Le modèle à suivre est `src/main/pomodoroOverlay.ts` et `src/renderer/src/screens/PomodoroOverlay.tsx`. **Les lire tous les deux avant d'écrire** : fenêtre cachée créée au démarrage, `frame: false`, `transparent`, `skipTaskbar`, `setAlwaysOnTop(…, 'screen-saver')`, fermeture interceptée en `hide()` avec le garde `isQuitting`, et côté renderer une route hors de `AuthProvider` qui ne fait que relayer l'IPC — **jamais d'appel Supabase depuis la fenêtre widget** (un second client Supabase rafraîchirait la même session persistée et provoquerait des déconnexions aléatoires ; le commentaire de `App.tsx` documente cet incident).

- [ ] **Step 1: Créer la fenêtre**

Créer `src/main/agendaWidget.ts` sur le modèle exact de `pomodoroOverlay.ts` :

- dimensions 300 × 260, positionnée en bas à droite de la zone de travail ;
- `createAgendaWidget(): void` qui construit la fenêtre cachée, charge la route `#/agenda-widget`, et branche :
  - `ipcMain.on('agenda:state-changed', (_e, items) => widgetWindow?.webContents.send('agenda:state', items))`
  - `ipcMain.on('agenda:set-visible', (_e, visible: boolean) => { if (visible) widgetWindow?.show(); else widgetWindow?.hide(); })`
- `export function toggleAgendaWidget(): boolean` qui bascule la visibilité et renvoie l'état résultant — utilisé par le menu du tray ;
- même garde `isDestroyed()` que celui ajouté à l'overlay, et le ref remis à null sur `closed`.

- [ ] **Step 2: Brancher au démarrage et au tray**

Dans `src/main/index.ts`, appeler `createAgendaWidget()` à côté de `createPomodoroOverlay(…)`.

Dans `src/main/tray.ts`, ajouter une entrée au menu contextuel, avant le séparateur :

```ts
      {
        label: "Afficher l'agenda du jour",
        type: 'checkbox',
        checked: false,
        click: (item) => {
          item.checked = toggleAgendaWidget();
        },
      },
```

en important `toggleAgendaWidget` depuis `./agendaWidget`.

- [ ] **Step 3: Exposer les deux canaux dans le preload**

Dans `src/preload/index.ts`, objet `api`, un sous-objet `agenda` calqué sur `pomodoro` :

```ts
  agenda: {
    // Fenêtre principale -> main -> widget.
    reportState: (items: { id: string; name: string; scheduledAt: string; done: boolean }[]): void => {
      ipcRenderer.send('agenda:state-changed', items);
    },
    // Widget : écoute les instantanés relayés par main.
    onState: (
      callback: (items: { id: string; name: string; scheduledAt: string; done: boolean }[]) => void
    ): (() => void) => {
      const listener = (
        _event: Electron.IpcRendererEvent,
        items: { id: string; name: string; scheduledAt: string; done: boolean }[]
      ) => callback(items);
      ipcRenderer.on('agenda:state', listener);
      return () => ipcRenderer.removeListener('agenda:state', listener);
    },
  },
```

Déclarer le même sous-objet dans `SaintDailyApi` (`src/renderer/src/env.d.ts`), en introduisant un type nommé `AgendaWidgetItem` que le renderer importe depuis `../env`.

**Le preload ne peut pas importer ce type** : il est compilé sous `tsconfig.node.json`, dont l'`include` ne couvre pas `src/renderer/src/**`. Il redéclare donc la forme en ligne, exactement comme il le fait déjà pour `PomodoroStateSnapshot` — dont le commentaire en tête de fichier documente cette contrainte et la nécessité de resynchroniser à la main. Ajouter la même mention pour ce nouveau type.

- [ ] **Step 4: Alimenter le widget depuis la fenêtre principale**

Créer `src/renderer/src/hooks/useAgendaWidgetFeed.ts` :

```ts
import { useEffect } from 'react';
import { getSupabaseClient } from '../lib/supabase';
import { endOfDay, startOfDay } from '../lib/calendarLayout';

// Même cadence que les deux autres sondages de l'app. Le widget doit
// refléter une tâche cochée il y a une minute, mais pas à la seconde près.
const REFRESH_INTERVAL_MS = 60_000;

interface AgendaRow {
  id: string;
  name: string;
  scheduled_at: string;
  deleted_at?: string | null;
}

/**
 * Pousse l'agenda du jour vers la fenêtre widget. Monté une seule fois,
 * dans la fenêtre principale — le widget ne parle jamais à Supabase.
 */
export function useAgendaWidgetFeed(): void {
  useEffect(() => {
    let cancelled = false;

    async function refresh() {
      const supabase = getSupabaseClient();
      const now = new Date();
      // `select('*')` puis tri côté client : nommer `deleted_at` ferait
      // échouer la requête tant que la migration de la corbeille n'est pas
      // appliquée à la base live.
      const { data, error } = await supabase
        .from('engagement')
        .select('*')
        .is('archived_at', null)
        .gte('scheduled_at', startOfDay(now).toISOString())
        .lte('scheduled_at', endOfDay(now).toISOString())
        .order('scheduled_at', { ascending: true });
      if (cancelled || error) return;
      const rows = ((data ?? []) as AgendaRow[]).filter((row) => !row.deleted_at);
      if (rows.length === 0) {
        window.api?.agenda?.reportState?.([]);
        return;
      }
      // « Fait » = possède au moins une entrée de pratique, la définition
      // qu'utilise déjà tout le reste de l'app.
      const { data: entries } = await supabase
        .from('practice_entry')
        .select('engagement_id')
        .in('engagement_id', rows.map((row) => row.id));
      if (cancelled) return;
      const done = new Set(((entries ?? []) as { engagement_id: string }[]).map((e) => e.engagement_id));
      window.api?.agenda?.reportState?.(
        rows.map((row) => ({
          id: row.id,
          name: row.name,
          scheduledAt: row.scheduled_at,
          done: done.has(row.id),
        }))
      );
    }

    void refresh();
    const interval = setInterval(refresh, REFRESH_INTERVAL_MS);
    return () => {
      cancelled = true;
      clearInterval(interval);
    };
  }, []);
}
```

Le monter dans `EngagementWatchers` (`src/renderer/src/App.tsx`), à côté des deux autres.

- [ ] **Step 5: Écrire le rendu du widget**

Créer `src/renderer/src/screens/AgendaWidget.tsx`, calqué sur `PomodoroOverlay.tsx` — même bandeau déplaçable, mêmes jetons de couleur, aucun accès aux données :

```tsx
import { useEffect, useState } from 'react';
import type { AgendaWidgetItem } from '../env';

function formatTime(iso: string): string {
  return new Date(iso).toLocaleTimeString('fr-FR', { hour: '2-digit', minute: '2-digit' });
}

// Ne passe JAMAIS par les hooks de données : cette fenêtre est un pur
// relais d'affichage, comme l'overlay Pomodoro. Un second client Supabase
// rafraîchirait la même session persistée que la fenêtre principale.
export default function AgendaWidget() {
  const [items, setItems] = useState<AgendaWidgetItem[]>([]);

  useEffect(() => window.api?.agenda?.onState?.(setItems), []);

  return (
    <div
      className="flex h-screen w-screen flex-col gap-2 border border-ink-700 bg-ink-900/95 px-4 py-3 [-webkit-app-region:drag]"
      style={{ borderRadius: 16 }}
    >
      <p className="font-data text-[10px] uppercase tracking-[0.1em] text-muted">Aujourd'hui</p>
      {items.length === 0 ? (
        <p className="text-[13px] text-muted">Rien de planifié aujourd'hui.</p>
      ) : (
        <ul className="flex flex-col gap-1.5 overflow-y-auto">
          {items.map((item) => (
            <li key={item.id} className="flex items-baseline gap-2.5">
              <span className="shrink-0 font-data text-[11px] tabular-nums text-muted">
                {formatTime(item.scheduledAt)}
              </span>
              <span
                className={`min-w-0 flex-1 truncate text-[13px] ${item.done ? 'text-muted line-through' : 'text-champagne'}`}
              >
                {item.name}
              </span>
            </li>
          ))}
        </ul>
      )}
    </div>
  );
}
```

- [ ] **Step 6: Déclarer la route**

Dans `src/renderer/src/App.tsx`, à côté de `/pomodoro-overlay` et **au même niveau** — c'est-à-dire hors de `AuthProviderLayout` :

```tsx
      <Route path="/agenda-widget" element={<AgendaWidget />} />
```

- [ ] **Step 7: Vérifier**

Run: `npm run typecheck`, `npm test -- --run`, puis `npm run build`
Expected: tout vert, build electron-vite propre (elle compile aussi le process principal et le preload, ce que le typecheck seul ne couvre pas entièrement).

- [ ] **Step 8: Commit**

```bash
git add src/main/agendaWidget.ts src/main/index.ts src/main/tray.ts src/preload/index.ts src/renderer/src/env.d.ts src/renderer/src/screens/AgendaWidget.tsx src/renderer/src/App.tsx src/renderer/src/hooks/useAgendaWidgetFeed.ts
git commit -m "feat: add a desktop agenda widget fed from the main window"
```
